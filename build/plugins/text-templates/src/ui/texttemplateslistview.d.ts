export default class TextTemplatesListView extends View<HTMLElement> {
    constructor(locale: any, dataList: any);
    items: ListView;
    _createList(dataList: any): ListView;
}
import { View } from '@ckeditor/ckeditor5-ui';
import { ListView } from '@ckeditor/ckeditor5-ui';
